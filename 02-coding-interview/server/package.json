{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend server with Express and Socket.io for coding interview platform",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test --test-force-exit tests/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1",
    "supertest": "^7.0.0"
  }
}
